import { expect } from "chai";
import { TypeGenerator } from "../src/typegeneration/typeGenerator";

describe("Typer", () => {
    it("Should work", () => {
        expect(true).to.equal(true);
    });
});
